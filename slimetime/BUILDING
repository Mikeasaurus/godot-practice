Checklist when building:

- Check for fonts, make sure appropriate ones are in "fonts" directory.
- Run "git stash" to remove any local overrides that were used for testing.
- Make sure version is updated in Globals.
- Ensure rcedit is configured for Windows builds.
- Add downloadable versions of Windows and Linux builds to root of web build.
  - slimetime-vX.Y.Z.exe for Windows, slimetime-vX.Y.Z.x86_64 for Linux.
- For headless server builds, ensure max fps is set to 60.
- For HTML5 builds, enable "Experimental Virtual Keyboard".
